# YOLO Label Tool 功能与界面说明

更新时间：2026-05-30

本文档记录当前项目已经实现的功能、界面结构、模块划分、配置文件位置，以及仍处于占位或未完成状态的能力。

## 项目结构

- Rust 后端入口：`src/main.rs`
- Rust Bridge 接口：`src/api.rs`
- Flutter 前端入口：`flutter/lib/main.dart`
- 标注页面：`flutter/lib/LabelPage.dart`
- 训练页面：`flutter/lib/TrainPage.dart`
- 浏览/视频检测页面：`flutter/lib/DetectVideoPage.dart`
- 标注数据模型：`flutter/lib/AnnotationModels.dart`
- 快捷键模型：`flutter/lib/ShortcutModels.dart`
- 配置读写模块：`flutter/lib/ConfigStore.dart`
- 设置弹窗：`flutter/lib/SettingsDialog.dart`
- 导出弹窗：`flutter/lib/ExportDialog.dart`
- 裁剪页面：`flutter/lib/CropPage.dart`
- 语言包目录：`flutter/lib/language/`
- Flutter Rust Bridge 配置：`flutter_rust_bridge.yaml`

当前 Flutter 代码使用 `part` 方式拆分模块，`main.dart` 负责应用壳层、全局状态协调和页面切换，具体页面和模型逻辑拆到独立文件中。

## 启动与桥接

- Rust 侧通过 Flutter Rust Bridge 暴露基础接口。
- Flutter 启动时会初始化 `RustLib`。
- 当前桥接状态用于获取支持的标注模式：`HBB`、`OBB`、`SEG`。
- `src/main.rs` 作为开发启动器，用于构建 Rust 并启动 Flutter Windows GUI。

## 顶部菜单栏

顶部菜单栏支持自动收起：

- 默认显示菜单栏。
- 3 秒无交互后自动收起为顶部细条。
- 鼠标移动到顶部菜单栏位置后自动展开。

当前菜单包括：

- 文件
  - 打开文件
  - 打开文件夹
  - 打开最近的文件
  - 退出
- 编辑
  - 撤销
  - 恢复
  - 复制
  - 粘贴
- 设置
  - 语言
  - 首选项
  - 清空缓存
- 说明
  - 快捷键说明
  - 关于

编辑菜单当前已接入标注框层面的撤销、恢复、复制、粘贴，不是系统剪贴板级别的通用编辑能力。

## 配置文件

配置文件存储在当前 Windows 用户目录下，不写死用户名：

```text
%USERPROFILE%\.rustlabel\history.json
%USERPROFILE%\.rustlabel\keybindings.json
%USERPROFILE%\.rustlabel\settings.json
```

当前保存内容：

- `history.json`：最近打开的文件夹和文件。
- `keybindings.json`：自定义快捷键。
- `settings.json`：Python 环境路径、训练结果保存位置。

设置弹窗当前支持：

- 查看配置目录。
- 设置 Python 环境路径。
- 设置训练结果保存位置。
- 显示当前配置目录文件大小。
- 清空历史、类别和临时标注数据。

## 文件与最近记录

支持的图片格式：

- `jpg`
- `jpeg`
- `png`
- `bmp`
- `gif`
- `webp`

打开逻辑：

- 打开文件时，如果图片已经在当前图片列表中，不会重复插入，只会选中已有图片。
- 打开文件夹时，会读取该文件夹内支持格式的图片。
- 已经存在于历史记录中的文件或文件夹，不会重复写入历史记录。
- 历史记录按路径去重，Windows 路径大小写差异不会产生重复记录。

最近记录：

- 最近文件夹和最近文件分别保存。
- 每类历史记录最多保存 20 条。
- 菜单中每类默认显示前 5 条。
- 超过 5 条时才显示“更多选项”二级菜单。
- 可清除最近打开记录。

## 图片排序

打开文件夹后，图片按自然排序显示。

示例顺序：

```text
001
002
010
011
021
```

```text
图片01
图片02
图片10
```

当前排序规则：

- 文件名中的阿拉伯数字片段按数值比较。
- 非数字文字片段按普通字符串比较。
- 数值相同时用原始字符串和完整路径兜底，保证排序稳定。

暂未处理的特殊情况：

- 中文数字，如“一、二、十”。
- 小数、负数。
- 按 EXIF 拍摄时间排序。
- 按文件修改时间排序。

## 左侧侧边栏

左侧侧边栏支持展开和收起。

当前导航项：

- 标注
- 训练
- 浏览

点击不同导航项会切换到对应页面。

## 标注页面

标注页面由三部分组成：

- 左侧图片预览区
- 中间标注工作区
- 右侧 AI/工具栏和类别管理区

### 图片预览区

图片预览区功能：

- 显示当前图片索引和图片总数。
- 显示图片缩略图列表。
- 支持滚动浏览。
- 点击缩略图切换当前图片。
- 右键可添加图片或删除图片。
- 预览区宽度会随窗口宽度缩放，缩略图也会随之缩放。

### 标注工作区

标注工作区是中间图片显示区域，画布逻辑尺寸为 960×620，窗口不足时通过双向滚动条查看完整画布。

画布逻辑尺寸：

```text
960 x 620
```

显示逻辑：

- 画布逻辑尺寸固定为 960×620，窗口不足时通过双向滚动条查看完整画布。
- 图片会根据自身尺寸自动按比例缩放。
- 默认完整显示在标注工作区内。
- 底部缩放控制会叠加在自动适配后的显示比例上。
- 缩放比例大于 70% 且已打开图片时，标注区底部显示左右箭头按钮，右侧显示上下箭头按钮，用于平移查看放大后的局部区域。
- 当前画布显示有网格背景。
- 标注线条粗细不会随着图片缩放比例一起变粗或变细。

### 标注模式

当前支持基础交互：

- `HBB`：普通矩形框。
- `OBB`：旋转矩形框。
- `SEG`：通过逐点点击生成轮廓，多于 2 个点后点击初始点附近闭合生成多边形。

模式切换方式：

- 点击标注区顶部的 `HBB`、`OBB`、`SEG` 按钮。
- 使用快捷键：
  - `R`：HBB
  - `B`：OBB
  - `S`：SEG

当前已实现的标注交互：

- HBB/OBB 使用两次点击完成标注，不需要按住鼠标拖拽。
- HBB/OBB 第一次点击后进入草稿预览，移动鼠标可查看框体范围。
- HBB/OBB/SEG 绘制模式下，鼠标在图片显示区域内会显示十字虚线辅助定位。
- 十字虚线会根据鼠标所在图片区域的亮暗自动切换深色或浅色，便于在不同图片背景上看清位置。
- 选择功能下不会显示十字虚线。
- HBB/OBB 第二次点击后完成标注，并继续保持绘制模式。
- 只有主动点击选择按钮，或在没有草稿时按 `Esc`，才会回到选择模式。
- 只有一次点击还未完成标注时，按 `Esc` 会取消当前草稿，并继续保持当前标注模式。
- 完成标注时如果点击位置超出图片显示区域，坐标会自动回归到图片边缘。
- 标注坐标会限制在真实图片显示区域内，避免 YOLO 训练时出现超出范围或无效归一化坐标。
- 如果没有类别，首次绘制时会弹出类别名称输入框。
- 选择工具下点击标注可选中。
- 选中标注后可以拖动移动。
- 选中 HBB/OBB 后可以拖动白色角点调整框体大小。
- 选中标注后外部区域会显示半透明灰色遮罩。
- 多个标注重叠时，连续点击会在命中的标注之间切换；都切换后会取消选择。
- `Del` 删除选中标注。
- `Esc` 在有草稿时取消草稿，在无草稿时回到选择状态。
- 长按 `A` / `D` 可以按更大步长快速切换图片。
- 复制必须先选中标注框才会生效。
- 复制成功后会显示悬浮小字提示，并渐渐消失。
- 右键标注会选中并弹出菜单，可删除、切换类别，OBB 可左右旋转。
- 右侧工具栏顶部可切换到当前图片所有标注列表。
- 点击标注列表中的标注项可以选中对应标注。
- 鼠标悬停标注列表项时可以查看该标注的画布坐标、中心点、尺寸和 SEG 点信息。
- 在标注列表中可以通过类别下拉框修改该标注的 class name。
- 选中标注时右侧会自动显示标注列表，取消选择后恢复工具页。
- OBB 支持快捷键旋转：
  - `Z`：逆时针 5°
  - `X`：逆时针 1°
  - `C`：顺时针 1°
  - `V`：顺时针 5°

当前还没实现的标注编辑细节：

- SEG 节点自由拖动编辑。
- SEG 节点选中后方形/圆形状态切换。
- 点击框体上方 class name 直接弹出下拉切换类别。
- class name 鼠标悬停背景变灰、字体变白的交互。

## 右侧 AI/工具栏

右侧工具栏当前包含：

- 选择
- 复制
- 粘贴
- 撤销
- 重做
- 删除

当前状态：

- 工具按钮支持选中态。
- 工具栏右上角提供工具页/标注列表切换。
- 复制、粘贴、撤销、重做、删除已接入当前标注数据。
- 撤销和恢复只针对标注框相关操作。
- AI 辅助标注、ONNX/pt/SAM 推理尚未接入。

## Classes 管理

右侧工具栏下方提供类别管理：

- 添加类别。
- 编辑类别名称。
- 删除类别。
- 点击色块选择类别颜色。
- 拖动类别项调整显示顺序。
- 点击类别项设置为当前绘制类别。
- 显示/隐藏标注框上方的类别名称。

颜色策略：

- 新类别会自动从预设色板中分配颜色。
- 当前色板避免纯黑、纯白、柠檬黄等不适合长时间观看的颜色。
- 类别名称文字颜色跟随类别颜色，选中时使用灰色背景和白色文字。
- 类别颜色选择器已经接入 `flex_color_picker`，支持色板、色轮和颜色代码输入。

当前类别和标注数据仍保存在运行时内存中，尚未持久化到 SQLite。

## 底部控制栏

底部控制栏当前包含：

- 缩小
- 当前缩放百分比
- 锁定缩放按钮
- 放大
- 重置
- 白天/夜间模式切换
- 自定义按键

交互：

- 鼠标滚轮可缩放图片。
- 缩放范围限制在 25% 到 400%。
- 重置按钮将缩放恢复到 100%。
- 锁定缩放开启后，鼠标滚轮、缩小、放大和重置按钮不会改变当前显示比例。

## 快捷键

当前默认快捷键：

- 上一张图片：`A`
- 下一张图片：`D`
- 放大图片：`=`
- 缩小图片：`-`
- HBB 标注框：`R`
- OBB 旋转框：`B`
- SEG 实例分割：`S`
- 删除选中标注：`Del`
- 显示/隐藏类别名：`H`
- OBB 逆时针 5°：`Z`
- OBB 逆时针 1°：`X`
- OBB 顺时针 1°：`C`
- OBB 顺时针 5°：`V`

固定编辑快捷键：

- 撤销：`Ctrl + Z`
- 恢复：`Ctrl + Y`
- 复制：`Ctrl + C`
- 粘贴：`Ctrl + V`

这些编辑快捷键当前只作用于标注框数据。

撤销/恢复记录只针对当前图片。切换图片后，撤销和恢复记录会清空。

自定义按键：

- 点击底部“自定义按键”按钮打开设置弹窗。
- 点击某一项后按下键盘按键即可记录。
- `Esc` 可取消当前按键录入。
- 支持重置为默认快捷键。
- 快捷键保存到 `%USERPROFILE%\.rustlabel\keybindings.json`。

## 训练页面

训练页面当前是前端骨架，已包含：

- 从项目 `models/` 文件夹读取 YOLO `.pt` 模型。
- 模型列表只显示以 `yolo` 开头的 `.pt` 文件，并排除 `-cls`、`-pose` 模型。
- 选择 dataset yaml。
- 开始训练按钮。
- 模型文件异常提示。
- 解析 `data.yaml` 中的 `names`、`train`、`val`。
- 显示 classes 数量、train 图片数量、val 图片数量。
- `train` / `val` 路径支持标量、行内数组和 YAML 列表，并根据 `data.yaml` 中的路径统计图片数量。
- 开始训练后会显示当前轮次。当前仍是前端状态预览，未接入真实 Ultralytics 训练进程。
- 训练曲线显示区域占位。
- 右侧超参数编辑面板。
- 右侧超参数面板 3 秒无交互后自动收起。
- 鼠标放在超参数上 0.5 秒显示参数说明。
- `batch` 支持固定批次、自动 60% 显存和显存比例三种模式。
- `imgsz` 改为固定滑动选择，只允许 `320`、`416`、`640`、`800`、`960`、`1280`，保证输入尺寸能被 32 整除。
- `amp` 使用开关表示，关闭传 `0`，开启传 `1`。
- `device` 会尝试通过 `nvidia-smi` 自动读取 NVIDIA GPU，并支持多选 GPU。
- 训练页和预测页的模型与输出路径使用“模型路径:”和“训练结果路径:”前缀显示。
- 超参数说明区已经加入 YOLO 调参提示，重点说明 `batch`、`lr0`、`momentum`、`imgsz`。

当前默认超参数：

```text
epochs=300
batch=16
imgsz=640，可选 320/416/640/800/960/1280
device=cpu 或 device=[0, 1]
amp=0
lr0=0.01
momentum=0.937
hsv_s=0.25
hsv_v=0.5
translate=0.1
scale=0.25
shear=5
flipud=0
fliplr=0
degrees=0.0
```

当前限制：

- 还没有调用 Python 环境执行训练。
- PT 模型校验目前基于文件名和所在目录，不是真正读取模型结构。
- 超参数修改暂未持久化。
- 训练曲线暂未接入真实日志。

## 裁剪页面

裁剪页面当前已接入 FFmpeg 视频帧提取：

- 支持一次选择单个或多个视频文件。
- 支持 `mp4`、`avi`、`mov`、`mkv`、`wmv`、`flv`、`webm`。
- 支持每 N 帧提取一帧，范围为 `0-30`；`0` 表示提取全部帧，`1` 表示每帧都提取。
- 支持无损 PNG 输出。
- 支持 `JPEG`、`JPG`、`WEBP` 压缩输出，默认输出格式为 `JPEG`。
- 支持压缩比例设置，默认值为 `90`；JPEG/JPG 会根据压缩比例选择色度采样，WebP/PNG 会映射到对应压缩等级。
- 支持自动选择硬件解码器：优先 NVIDIA CUDA，其次 Intel Quick Sync Video，再其次 D3D11VA，最后回退到 CPU 解码。
- 点击开始提取时会弹窗选择保存路径，并确认输出文件夹名称。
- 开始提取后会先统计所有视频总帧数，再根据取帧间隔计算预计输出帧数，并通过保存路径中已提取图片数量刷新圆形旋转指示器和进度条。
- 输出文件名按视频序号和原视频文件名生成，避免多个视频文件名冲突。
- FFmpeg 查找顺序：
  - 环境变量 `FFMPEG_PATH`
  - 项目目录 `ffmpeg/bin/ffmpeg.exe`
  - 项目目录 `tools/ffmpeg/bin/ffmpeg.exe`
  - 系统 `PATH` 中的 `ffmpeg`

当前限制：

- 裁剪页当前通过 Flutter/Dart 启动 FFmpeg；Rust API 已保留 FFmpeg 提帧草稿，但 Flutter Rust Bridge 生成文件尚未接入该 API。

## 浏览 / 视频检测页面

浏览/视频检测页面当前已包含：

- 选择文件，文件选择器限定图片和视频格式。
- 选择文件夹。
- 播放视频开关。
- 预测视频开关。
- 全部预测开关，默认关闭。
- 播放和预测互斥，默认勾选播放。
- 保存结果开关只在“预测视频 / 全部预测 / 当前选择图片”时可用。
- 勾选预测时会弹窗选择 PT 模型文件，文件选择器默认打开训练结果保存位置。
- 选择文件夹后，播放区左侧显示媒体预览列表。
- 媒体预览列表 3 秒无交互后自动收起。
- 显示当前文件名。
- 显示图片文件时会直接在显示区域中预览图片。
- 视频播放后端通过 Rust 调用 FFprobe 读取宽高、时长、FPS，再调用 FFmpeg 解码指定时间点 PNG 帧给 Flutter 显示。
- 播放后端会自动选择硬件解码器：优先 NVIDIA CUDA，其次 Intel Quick Sync Video，再其次 D3D11VA，失败时回退 CPU。
- 视频显示区包含播放/暂停按钮、进度条、当前时间、倍速和后端解码状态。
- 预测状态下点击显示区域可切换“显示预测结果 / 隐藏预测结果”状态。
- 文件夹媒体预览中可用 `A` / `D` 切换媒体，长按时按更大步长快速切换。
- 视频播放/暂停、回退、三倍速快进已经加入自定义按键设置，默认分别为空格、左方向键、右方向键。
- 长按三倍速快进键时以 3 倍速快速播放，松开后恢复 1 倍速。
- 视频帧显示启用无缝切换，避免解码下一帧时出现短暂空白。
- 读取设置中的训练/检测结果保存位置并显示。
- 训练结果保存位置默认为项目目录下的 `Runs` 文件夹。

当前限制：

- 还没有调用 Python 环境执行 detect。
- 还没有接入真实推理结果叠加和预测视频导出。
- 预测视频导出还没有接入 NVIDIA 硬件编码。
- 还没有实现预测视频压缩导出。

## 主题

当前支持：

- 白天模式
- 夜间模式

夜间模式风格：

- 深紫色背景
- 白色文字
- 紫蓝色按钮、边框和高亮色

主要界面区域会跟随主题切换，包括：

- 顶部菜单栏
- 左侧侧边栏
- 图片预览区
- 标注工作区
- 右侧工具栏
- 底部控制栏
- 菜单和弹窗

## 多语言

语言包目录：

```text
flutter/lib/language/
```

当前语言包：

- `zh_cn.json`
- `en_us.json`

设置菜单会扫描 `flutter/lib/language/*.json` 并生成语言选项。

语言包字段示例：

```json
{
  "language.name": "简体中文",
  "menu.file": "文件",
  "menu.settings": "设置"
}
```

新增语言方式：

1. 在 `flutter/lib/language/` 中新增 JSON 文件，例如 `ja_jp.json`。
2. 保持字段 key 与现有语言包一致。
3. 设置 `"language.name"` 作为菜单中显示的语言名称。
4. 启动程序后会自动出现在 `设置 > 语言` 中。


## 需要修改的逻辑

当前仍需要继续修改或接后端的逻辑：

- SEG 当前生成的是多边形轮廓点；轮廓点转二值掩码图还没有后端实现。
- 训练页已经读取 models 文件夹、data.yaml 概览和训练参数，但还没有调用 Python/Ultralytics 开始训练。


## 当前占位或未完成能力

以下功能已有入口或规划，但尚未实现完整业务逻辑：

- Rust 标注逻辑后端化。
- SQLite 存储历史文件、文件夹、标注坐标、类别索引和颜色。
- 文件或文件夹移动后自动清理数据库失效数据。
- Python 环境自动搜索 conda、uv 虚拟环境。
- Python 环境 CUDA 自动测试。
- PyTorch、ONNX Runtime 版本和 GPU 信息检测。
- 调用 Python/Ultralytics 执行真实训练。
- 训练日志解析和真实曲线绘制。
- 根据官方模型自动匹配 ultralytics cfg yaml。
- 模型导出。
- 浏览/视频检测真实推理。
- NVIDIA 硬件编解码和预测视频压缩导出。
- 标注数据导入导出后端化与更多格式支持。
- 按类别数量尽量均衡分配数据集。
- 导出成功后跳转训练页并自动开始训练。
- 训练任务管理。
- ONNX 辅助标注。
- YOLO PT 辅助标注。
- SAM 辅助标注。

## 已考虑的实现细节

- 打开文件和打开文件夹时避免重复图片和重复历史记录。
- 最近记录按路径归一化去重。
- 文件夹图片排序使用自然排序，更符合数据集文件名习惯。
- 菜单栏自动收起后仍保留顶部触发区，方便重新显示。
- 文案从语言包读取，便于后续扩展英文或其他语言。
- 页面代码已拆分，便于继续扩展标注、训练、浏览模块。
- 标注模型、快捷键模型和配置存储已拆分为独立模块。
- 已通过 `dart pub get --offline`、`dart format`、`dart analyze` 和 `cargo check` 验证。
