// ignore_for_file: file_names

part of 'main.dart';

/// 训练页面占位，后续放置数据集、模型和训练参数配置。
/// Placeholder for training workflows such as dataset, model, and parameter setup.
class _TrainPage extends StatelessWidget {
  const _TrainPage();

  @override
  Widget build(BuildContext context) {
    return const Expanded(child: SizedBox.expand());
  }
}
